-- Disabled for local replay: enrolls a specific production student/subject
-- id (a one-off dashboard fix for a real user) that doesn't exist in a fresh
-- local database. Not depended on by later migrations.
SELECT 1;
