-- Disabled for local replay: yet another retry of the same
-- principal@school.edu + teacher1-5@school.edu demo roster (this one via
-- create_demo_user()), colliding with 20250809053652's already-seeded data.
SELECT 1;
