-- Disabled for local replay: one-off production data patch enrolling
-- "Alex Martinez" into a specific hardcoded class id that only exists on
-- the hosted project. Not depended on by later migrations.
SELECT 1;
